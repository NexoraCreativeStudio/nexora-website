# Nexora — payment integration environment variables (PLACEHOLDERS ONLY)
# Copy to .env locally or to the hosting provider's env-var settings when activated.
# NEVER commit real values. .env is git-ignored.

# Stripe (test mode first, production only after Owner activates the account)
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=

# Hosting / function environment (e.g. Netlify)
# NETLIFY_SITE_ID=
